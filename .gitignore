# Python
__pycache__/
*.py[cod]
*.so
*.egg-info/
dist/
build/
.eggs/

# Rust
/target/

# Caches
.mypy_cache/
.ruff_cache/
.pytest_cache/

# IDE
.vscode/
.idea/

# OS
.DS_Store

# Archive
/archive/

# Environments
.env
.venv/

# Coverage artifacts
.coverage
coverage.xml
coverage.svg

# Scratch / reference materials
/ref/

# Benchmark results and reports
/results/
# `/reports/*` rather than `/reports/`: git cannot re-include a file whose parent
# DIRECTORY is excluded, so excluding the directory would make the negation below a
# silent no-op.
/reports/*
/discopt_benchmarks/reports/
# ...except the Phase 0 Regime-N baselines. These are not run output, they are the
# frozen REFERENCE every Regime-N card is gated against (consolidation plan §0.1):
# `panel_baseline.py --check reports/panel_baseline_<sha>.json` is meaningless if
# the baseline is not in the repository at the sha it names.
!/reports/panel_baseline_*.json
# ...and the Phase 5 coverage census, for the same reason: it is the ranking that
# ORDERS the Phase-5 sub-cards (plan Phase 5.1), so a later card must be able to
# read the table it is sequenced against rather than re-derive it.
!/reports/phase5_kernel_coverage_census_*.json
# ...and the Phase 5.4 differential panel, which is the Regime-C evidence for the
# DISCOPT_CONVEX_KERNEL graduation decision recorded in the plan's §6.
!/reports/phase5_convex_kernel_diff_panel_*.json
!/reports/phase5_convex_kernel_budget_entry_*.json

# Generated course material (built from python/discopt/course/)
/course/

# Build output
_build/

# Task files
tasks.org

# LaTeX build artifacts
manuscript/_minted/
manuscript/*.aux
manuscript/*.bbl
manuscript/*.bcf
manuscript/*.blg
manuscript/*.dvi
manuscript/*.fdb_latexmk
manuscript/*.fls
manuscript/*.log
manuscript/*.out
manuscript/*.run.xml
manuscript/*.synctex.gz
manuscript/*.toc
# discopt.tex is generated from discopt.org via `scimax export discopt.org --format pdf`
manuscript/discopt.tex

# Claude Code
.claude/

# Crucible knowledge base: track the wiki and bib, exclude generated /
# copyrighted / transient artifacts.
.crucible/crucible.db
.crucible/references.bib.lock
.crucible/sources/external/
.crucible/wiki/viz.html

# Adversary test runs
adversary/

# Jupyter Book
docs/_build/

# DoE demo workbook artifacts
docs/notebooks/*.xlsx

# Solver-written .sol files (POUNCE / AMPL solvers like couenne/scip write a
# <stub>.sol next to the input .nl when run over the vendored instance tree).
# These are run artifacts, never fixtures (0 are tracked) — keep them out.
*.sol

# stray solver listing files (GAMS/BARON .lst) — not part of the repo
*.lst

# scratchpad run logs (keep the harnesses, drop the outputs)
scratchpad/*_out*.txt
scratchpad/*.log

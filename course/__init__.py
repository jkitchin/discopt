"""discopt course package marker.

This file exists so notebooks can ``from course._compat import *`` to install
the pedagogical-API shim onto ``discopt.Model``.

It is intentionally minimal — the course is content, not code.
"""

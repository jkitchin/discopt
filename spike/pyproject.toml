[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "discopt_spike"
requires-python = ">=3.10"
version = "0.1.0"

[tool.maturin]
features = ["pyo3/extension-module"]

"""Readers that load trained ML models (networks, trees, ensembles).

Each reader converts a third-party trained model — ONNX, scikit-learn, PyTorch —
into a discopt ``NetworkDefinition`` or ``TreeEnsembleDefinition``.
"""
